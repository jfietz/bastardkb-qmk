## 2024-05-22 - [Build Summary Failure List]
**Learning:** Users prefer immediate, actionable feedback in CLI tools. Listing specific failed items in the summary panel prevents users from scrolling through large logs to identify failures.
**Action:** When summarizing batch operations in CLI tools, always list the specific items that failed if the count is manageable, rather than just the failure count.
